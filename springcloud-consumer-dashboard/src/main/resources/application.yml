server:
  port: 1004
spring:
  application:
    name: springcloud-consumer-dashboard
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka/
management:
  endpoints:
    web:
      exposure:
        include:
        - hystrix.stream
